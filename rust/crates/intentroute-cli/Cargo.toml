[package]
name = "intentroute-cli"
description = "Command-line companion for IntentRoute AI rule files, built on the Rust core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
intentroute-core = { path = "../intentroute-core" }
serde = { workspace = true }
serde_json = { workspace = true }
